FROM python:3.11-slim

WORKDIR /app

# System dependencies (extend as needed, e.g. for BLAS, LAPACK, etc.)
RUN apt-get update && apt-get install -y \
    build-essential \
    && rm -rf /var/lib/apt/lists/*

COPY ../../ /app

# TODO: Replace with your actual requirements file.
# RUN pip install --no-cache-dir -r requirements.txt

CMD ["bash"]
